# Mosquito 固件开发与 Agent 交接规则

本文件适用于整个仓库。最终目标是：任何开发者或 Agent 在 WSL2 ext4 中克隆 GitHub `main` 后，仅依靠仓库内文档就能判断当前事实、接续任务，并严格遵守实板验证和新镜像生成门槛。Windows 继续提供 ADB、UART 和烧录能力，但不维护第二份板端仓库。

## 一、权威文档与阅读顺序

每次开始工作、发生上下文压缩、切换电脑或换 Agent 后，必须按顺序完整阅读：

1. `AGENTS.md`：不可绕过的工作规则。
2. `docs/CURRENT_STATE.md`：当前源码、镜像、实板验证、已知问题和风险。
3. `docs/NEXT_IMAGE.md`：现阶段下一步、验收矩阵及新镜像准入条件。
4. `docs/DEVELOPMENT_WORKFLOW.md`：WSL2、Windows 硬件桥接、Git 和自动测试闭环。
5. `README.md`、`tina-overlay/MOSQUITO_BUILD.md`：项目使用与构建背景。
6. 当前版本的 `releases/mosquito-t113/<版本>/README.md`。

若文档互相冲突，以“更保守、不会生成新镜像、不会破坏现有数据”的解释为准，并先向用户报告冲突。不得凭聊天记忆覆盖仓库中的明确事实；发现事实变化时，应在本次工作结束前同步更新 `CURRENT_STATE.md` 和 `NEXT_IMAGE.md`。

## 二、WSL2 克隆后的首次检查

Agent 必须运行在 WSL2 中，并先确认自己位于 ext4 上的正确仓库和分支。可在 WSL shell 中运行：

```sh
echo "$WSL_DISTRO_NAME"
uname -a
pwd
git remote -v
git branch --show-current
git status --short --branch
git rev-parse HEAD
cat AGENTS.md
cat docs/CURRENT_STATE.md
cat docs/NEXT_IMAGE.md
```

预期路径位于 `/home/...` 而不是 `/mnt/c/...`，远端是 `https://github.com/skyjane1129-blip/mosquito-t113.git`，正常接手分支是 `main`。若环境、远端、分支或工作区状态不同，不得自动丢弃、覆盖、提交或合并现有修改；先识别这些修改属于谁、是否与当前任务相关。

板端仓库、完整 Tina SDK、Linux 工具链和编译输出都放在 WSL2 ext4；仓库只由 WSL Git 写入。Windows 负责 ADB、USB/UART 驱动、PhoenixCard、串口终端和少量产物暂存。不得使用 Windows Git 经 `\\wsl$` 写入该仓库，也不得在 Windows 和 WSL2 各维护一份可写 checkout。不得假定仓库包含约 17 GB 的第三方 Tina SDK、下载缓存或工具链，也不得把这些内容提交到本仓库。

推荐目录为 `~/work/mosquito/mosquito-t113` 和同级的 `~/work/mosquito/tina-t113`。WSL2 中的 Agent 可直接调用 Windows `adb.exe` 或固定 PowerShell 脚本测试开发板；详细边界见 `docs/DEVELOPMENT_WORKFLOW.md`。

仓库的 `.gitattributes` 用于保护 Shell 脚本及 Linux 配置的 LF 行尾。不得用会批量改写行尾或可执行位的工具保存整个 `tina-overlay/`。

## 三、新问题的默认阶段

出现新问题时，默认处于“当前已烧录镜像的诊断与验证阶段”，不是“制作下一镜像阶段”。

- 先运行 `mosquito-version`，记录实际运行镜像、构建日期和板级包版本。
- 在当前已经烧录、正在运行的系统镜像上复现并定位问题。
- 保留能够证明根因的日志、状态、命令、退出码、时间点和硬件连接方式。
- 优先使用临时命令、临时脚本、`/tmp`、`/overlay` 或其他现有可写区域验证修复。
- 不能只根据源码、静态检查、成功编译或成功 `pack` 推断实板修复有效。
- 当前镜像无法复现时，不得为“试试看”直接做新镜像；先检查环境、设备版本和复现步骤。

没有实板连接、没有串口/ADB 通道或无法取得当前镜像版本时，可以继续做只读源码分析和文档整理，但必须把结论标为“源码推断”或“待实板验证”，不能写成已验证。

## 四、新镜像生成门槛

只有依次完成以下关卡，才可以向用户申请制作新镜像：

1. **复现**：在当前镜像上稳定复现，保存原始证据。
2. **定位**：证据能够解释根因，而不只是描述现象。
3. **临时修复**：在当前镜像可写层上实施最小修复。
4. **修复验证**：重复原复现步骤，证明问题消失。
5. **回归检查**：检查受影响功能、相邻功能及当前主要任务关键路径。
6. **风险汇总**：向用户说明根因、当前镜像修复结果、回归结果和尚存风险。
7. **明确询问**：必须明确询问用户是否开始制作新版本系统镜像。

只有用户明确同意后，才允许：

- 修改镜像版本信息或板级包版本；
- 把临时修复写入固件源码；
- 启动 Tina `make`、镜像构建或 `pack`；
- 建立新的版本发布目录；
- 复制、改名、上传或发布新的系统镜像。

未获得明确同意时，继续停留在当前镜像的诊断、验证或补充测试阶段。用户允许整理 GitHub、文档、分支或已有版本资料，不等于允许制作新镜像。

## 五、证据和状态用语

所有记录必须区分以下状态：

- **实板已验证**：明确指出在哪个实际镜像和硬件上运行了什么命令，并保存结果。
- **临时修复已验证**：修复只存在当前镜像的 overlay 或临时路径，尚未证明最终成品镜像包含并正确运行。
- **成品静态验证**：编译、`pack`、SHA-256、SquashFS 提取或文件反查通过，但不等于实板通过。
- **候选**：允许继续验收，不代表稳定或生产可用。
- **待验证**：没有足够证据，不能推定通过。
- **已知故障/禁止烧录**：必须在版本 README 顶部显著标记，必要时保留 `DO_NOT_FLASH.txt`。

建议每条实板证据至少记录：主机日期与时区、硬件连接、运行镜像的 `mosquito-version` 输出、完整命令、关键原始输出、退出码、相关文件 SHA-256、重复次数及失败次数。日志涉及 ICCID、精确坐标、上传 URL、令牌或其他敏感信息时必须脱敏。

## 六、回归范围

修复验证后，至少检查与改动相邻的路径。当前主要路径包括：

- TF 冷启动、双核、根文件系统和可写 overlay；
- UART0 Root Shell 与 ZMODEM 救援通道；
- USB0 `adb-on`、ADB stop/start、热拔插、`shell/push/pull`；
- USB1 UVC 摄像头供电、拍照、JPEG/元数据和 TF 同步；
- Air780EG LTE 注册、PPP、DNS、NTP、HTTPS 上传和 `4g-stop`；
- ADB 与 4G 并行运行；
- SIM、GNSS、BQ25895 只读诊断；
- v5.6.4 晚期自动 ADB、失败后的 UART/`adb-on` 救援能力以及开发安全边界。

无法执行的项目必须明确标为未测，并解释原因；不得用“应该没问题”代替。

## 七、Git 与 `main` 规则

- `main` 表示当前集成开发基线，不自动等同于生产稳定镜像。
- `main` 中的源码、`CURRENT_STATE.md`、`NEXT_IMAGE.md` 和当前发布说明必须一致。
- 功能开发和诊断必须在短生命周期主题分支进行，推荐使用 `feature/`、`fix/`、`test/`、`docs/`、`infra/`、`release/` 前缀；只有证据、文档和文件范围审核完成后才通过 PR 进入 `main`。
- 不得强制推送、重写已发布历史或把多个来源不明的工作区修改一次性盲目提交。
- 不得清理用户现有修改。提交前必须检查 `git diff --check`、`git status`、脚本语法和版本引用。
- 没有用户要求时，不自动创建提交、标签、GitHub Release 或推送远端。
- 将某一版本设为 `main` 不等于授权生成该版本之后的新镜像。
- Windows 客户端使用独立的 NTFS 工作树、`mosquito-windows-client` 分支和 Windows Git；它可以与板端分支共用同一个 GitHub 仓库，但不能成为本板端工作树在 NTFS 上的第二份副本。

### 新传入文件的推送确认门槛

- 任何新传入或新增到工作区的文件，包括用户上传、外部复制、下载、工具生成以及 Agent 新建的文件，都必须在推送前单独盘点。
- 盘点至少列出文件路径、来源、类型、字节数、Git 跟踪/忽略状态、是否可能包含隐私/凭据/第三方受限内容，以及计划放入仓库的位置。
- 执行包含这些新文件的任何 `git push` 前，必须把盘点清单交给用户并明确询问是否允许推送；只有取得用户明确确认后才能推送。
- 未取得明确确认时，可以进行只读检查和本地整理，但不得把包含这些新文件的提交推送到任何远端。
- 内容和 SHA-256 均未改变的已跟踪文件重命名不属于“新传入文件”，但仍应在提交或推送汇总中报告原路径和新路径。
- 每次推送前必须检查 `git status --short` 和待推送提交的新增文件清单，不能仅凭记忆判断是否出现新文件。

## 八、镜像目录与 GitHub 发布规则

所有板级镜像资料统一位于：

```text
releases/mosquito-t113/<YYYY-MM-DD>-<版本>-<用途>/
```

本地版本目录可包含实际 `.img`，但 `.img` 由 `.gitignore` 排除，不直接进入 Git 历史。GitHub 仓库提交以下可审计资料：

- `README.md`：版本身份、目的、来源、变更、状态、烧录方式、验收结果和风险；
- `<镜像名>.img.sha256`：精确文件名和 SHA-256；
- 必要的脱敏日志、测试矩阵或 `DO_NOT_FLASH.txt`。

正式编号且需要长期保留的镜像，待用户授权上传后，应作为对应 Git tag 的 GitHub Release 附件保存。临时构建、失败中间产物和未编号试验文件放在 `/tmp` 或本地构建目录，不创建发布版本。不得覆盖同名镜像；内容变化必须使用新版本号和新目录。

GitHub Release 附件不会自动出现在普通 `git clone` 的文件夹中。Windows 需要烧录历史镜像时，应从对应 Release 下载到同名本地目录并执行：

```powershell
Get-FileHash .\releases\mosquito-t113\<版本>\<镜像>.img -Algorithm SHA256
```

结果必须与目录内 `.img.sha256` 一致。除非用户以后明确选择 Git LFS 方案，否则不得为了让克隆自动下载镜像而把 200 MB 以上的历史二进制加入源码仓库。

## 九、烧录与安全

- Mosquito 镜像是 Allwinner/PhoenixCard 整卡格式，不是普通 `dd` 镜像。
- 烧录会重建所选 TF 卡分区；操作前备份并再次核对目标盘符。
- 使用 PhoenixCard“启动卡 / Startup”模式，不使用 Etcher 或普通 `dd`。
- `dev-v1` 是已知循环重启镜像，禁止烧录。
- root、无认证 USB0 ADB 仅限受控开发环境，不能作为野外生产安全方案。
- 不提交密钥、口令、私人上传地址、未脱敏 ICCID、精确位置或第三方受限 SDK。

## 十、结束工作前

每次形成新的可靠事实后，都要同步更新：

- `docs/CURRENT_STATE.md`：已经发生并有证据支持的事实；
- `docs/NEXT_IMAGE.md`：仍需执行的下一步、门槛和验收项；
- `docs/DEVELOPMENT_WORKFLOW.md`：开发环境、Git 位置或硬件桥接规则发生变化时同步更新；
- 对应版本 `README.md`：该成品独有的身份、状态和证据。

最后向用户汇总：改了什么、没有改什么、验证了什么、未验证什么、当前风险、是否涉及新镜像，以及下一步是否需要用户授权。

## 十一、当前晚期自动 ADB 的实现与回归边界

v5.6.3 已实现晚期自动 ADB，v5.6.4 继续沿用该路径。以下约束仍然有效，也不构成制作后续镜像的授权：

- 保留 `adb-on` 作为唯一 ADB 底层启动流程，只在明确的“系统 userspace 已就绪”节点自动触发一次。
- 不得恢复 v5.5.x 在 `rc.preboot` 中提前准备、绑定或强制重绑 USB0 的实现，也不得另写一套与 `adb-on` 并行的 ConfigFS/FunctionFS 启动逻辑。
- 自动触发失败时不得重启系统或无限重绑 UDC；必须保留 UART、`adb-on` 手动恢复入口和 `usb0-adb-stop` 停止入口，并记录可诊断日志。
- v5.6.4 的当前剩余项是连续三次物理冷启动/UART、USB0 物理热拔插/正反插，以及 UART Root Shell 在场时的 ADB stop/start。不能用 v5.6.3 或更早版本的通过记录替代这些精确成品测试。
