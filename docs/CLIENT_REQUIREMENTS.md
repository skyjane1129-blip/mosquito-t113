# 客户端需求与本轮实现

更新：2026-09-08，Asia/Shanghai。依据项目负责人确认的第二版工作区，以及后续历史查询、上报异常、登录和工作区布局规则。本文件覆盖之前“远程立即加入主动控制”的旧范围；主动控制保留为后续需求。

## 当前范围

- 保留深蓝 #0B3A66、青绿 #0F8C8D、浅灰 #F3F6F8、白色圆角卡片及原有字体体系。
- 软件窗口和顶部名称统一为“智能诱蚊诱卵采集平台”。顶部登录区与紧凑的直连 / 远程模式按钮复用同一位置，移除原先高 104 DIP 的独立模式卡片行。
- 必须先通过工程师本地登录或云端登录才能操作工作区；本规则取代之前“完全不登录也可操作”的约定。
- 直连：Type-C / ADB 拍照、读取温湿度与电源、文件校验和本机保存；工程师本地登录即可使用，上传待传记录另需云端认证。
- 远程：板端每小时自动拍照、采样、上传，平时低功耗；客户端查看数据。该计划须在板端独立运行，不能依赖 Windows 是否打开。
- 暂不显示主动拍照、唤醒、主动读传感器、远程对焦入口。保留后续实现需求；未来手动采集属于额外采集，不重置每小时计划。
- 暂不加入任务入口 / 任务面板，也不加入 AI 检测框、蚊虫蚊卵数量。
- 两模式都显示稳定设备编号。编号来自设备登记或配置；ADB 序列号作为连接信息独立保留，未登记时显示待确认。

## 工作区

2026-09-07 入口简化：直连、远程工作区的“全部记录”按钮均删除，通用历史查询统一从顶部“记录查询”进入；查询页保留设备和时间筛选。

2026-09-08 工作区布局已按确认方向实现，保留两栏及原有配色、字体、圆角卡片：

- 直连左侧保留按原始比例显示的大图，“放大 / 原图”“保存照片”放在主图标题旁，删除右侧重复缩略图。
- 直连右侧分为“设备状态”“采集操作”“本次采集结果”。设备摘要和常用采集、读取按钮固定在上方，结果与长详情在自己的区域滚动；检测分项仍可展开并独立滚动。
- 采集结果突出显示采集时间、记录状态、温度、湿度、电池电压、充电状态及保存 / 接收状态，完整信息在“更多采集详情”展开；缺失数据不补造。
- “读取设备信息”的输出位于“实时读取结果 · 独立测量”，完成读取后自动展开。此结果不替换照片关联的采集数据。
- 远程右侧的设备编号、GNSS 和上报提示固定在上方。左侧看地图时，右侧显示照片预览、照片操作及最近采集结果；左侧看采集影像时，右侧重复预览与照片操作收起，操作入口放到左侧主图旁。
- 查看长记录不改变设备选择；切换地图 / 影像复用已加载照片，不因布局切换额外请求或清空照片。历史查询入口、筛选和上报检查逻辑保持原有约定。
- 布局验证覆盖 1500×960 与最小 1180×800 窗口；窗口较小时结果区需要滚动，常用采集按钮保持可见。

远程保留“设备地图 / 采集影像”。地图从上海全图进入行政区，再选择板子；右侧显示设备编号、最近采集和上次 GNSS 位置。相邻点以“聚合 · N 台”区别于具体编号，聚合可放大；同坐标设备可从列表选择。右侧编号下拉支持快速切换设备。

切换地图与影像保留设备与地图范围；切换设备加载该设备最新采集。历史照片在记录查询中独立选择，测量值始终对应同一张照片。直连手动读取的信息独立于历史采集快照。

当前地图是已有第二版行政区资产的 WPF 示意实现。设备目录接口未接入时不显示虚构设备或数量。只把明确匹配底图 GCJ02 的有效坐标画到图上；WGS84、未知坐标系、未定位设备仍可通过右侧编号列表查看原始位置。正式 GNSS 坐标转换与生产地图服务需要后续联调。

## 统一登录与工作区锁定

- 共用一组账号、密码输入框和一个登录按钮，无登录类型选择器。首次打开和退出后输入框为空，分别显示“账号”“密码”；密码掩码显示，支持回车登录。
- 当前保留账号 `admin` 给工程师本地入口，临时密码 `000`；本机校验，不依赖网络，可以操作真实开发板。密码错误保持锁定，不向云端发送工程师凭据。
- 其他账号调用现有 `POST /api/auth/login`，收到有效 `accessToken` 和未来的 `expiresAt` 后才进入云端会话。无真实云端测试账号时，以模拟接口验证客户端流程，真实认证联调仍待完成。
- 输入为空、认证失败或网络异常均不能解锁。未登录时展示“请先登录”，不查询业务记录、不访问开发板、不执行后台业务轮询；界面、命令及子页面同时检查会话权限。
- 登录成功默认直连，顶部显示“工程师模式”或“云端已登录 · 账号”，以及直连 / 远程按钮和“退出登录”。当前模式青绿高亮，保持原视觉风格。
- 工程师可以进入远程工作区查看结构及接入提示，云端设备、采集记录、上传与上报检查仍需云端会话，不注入模拟生产数据或伪造云端令牌。
- 退出后清除密码控件、会话、云端令牌及当前屏幕数据，关闭原图窗口并取消查询；本机 SQLite 和照片保留。旧异步响应不能恢复上次账号的数据。
- 云端 401 或令牌到期恢复锁定，显示过期提示；普通断网保留未过期会话和已查询快照。直连操作进行中暂禁主动退出和模式切换；若云端会话在本机采集中到期，工作区锁定，已开始的本机保存允许完成，旧结果不重新显示。
- 当前工程师凭据仅用于本阶段开发与现场调试，正式客户交付时的凭据管理仍需在发布阶段确定。

## 直连环境与设备检测（客户端已实现）

2026-09-07 项目负责人提出：接入 Type-C 后，客户端自动检查电脑是否满足数据传输要求，并自动检测开发板连接；需要覆盖没有预装 ADB 的新电脑。

- 登录成功进入直连、从远程切回直连时检查一次；Windows USB 变化通知触发防抖检查，并对启动中的板子进行有限重试，60 秒轮询作为补充。未登录不探测。
- 右侧“设备状态”标题旁显示“检测设备”，运行时为“检测中…”，不可重复发起；结果摘要常驻，分项和检查时间在“检测详情”展开并独立滚动。
- USB 发现独立于 ADB：通过 Windows PnP 识别候选设备、读取硬件标识和系统提供的描述信息、检查驱动问题。USB 未完成枚举时只能显示未发现或识别异常，不能凭空读取型号或编号。
- 当前分层检查 ADB 程序可运行性、USB 设备、WinUSB 驱动、ADB 只读握手、采集 / 环境 / 电源命令存在性、镜像声明的 metadata 版本及身份。目标按配置的 ADB 序列号匹配，发现重号不任意选择。
- 状态区分未发现目标、组件缺失、驱动待确认、ADB offline、板端响应异常、已连接但接口未就绪、有待确认项和检测通过。命令存在不等同于一次真实采集已验证；镜像声明与临时接口可能不同，仍须单独联调。
- 自检仅执行 USB 枚举、ADB version / devices、mosquito-version 和 command -v；不触发拍照、传感器采样、上传或修改板端系统。电脑不联网也能在工程师登录后检测。
- 原需求中的本地保存目录权限与空间预检、测试文件传输保留为后续补充；本轮没有把它们标记为已验证。
- USB 报告的产品字符串与序列号可能固定或过期。正式设备编号和真实固件版本需要通信握手核对；当前统一的 MOSQUITO-T113-DEV 不能作为量产每板唯一编号。

## 客户安装交付要求（已确认，安装包待实现）

- 最终客户端必须打包成可在客户 Windows 10/11 电脑上一键安装的安装包。
- 安装程序自动部署经过验证且允许分发的 ADB 主机组件、匹配依赖和客户端运行环境；软件使用应用内路径并按需启动 ADB 后台进程。客户无需手动安装 ADB、开发 SDK 或配置 PATH。
- 安装流程检查 USB 驱动，优先使用 Windows 内置 WinUSB 自动匹配；确需补装时引导安装匹配硬件的签名驱动，并由 Windows 显示必要权限确认。
- 已安装 ADB 的电脑不应被盲目替换全局工具；应用通信组件应可随客户端统一维护。
- 安装完成后可打开客户端、登录并检测板子。最终验收须覆盖无预装 ADB / .NET SDK 的干净电脑、已有 ADB 的开发电脑和驱动异常场景。
- 本轮更新上述交付要求并生成自包含程序，尚未制作安装器、随包部署 ADB 或执行干净电脑安装验收。

本轮只读证据：未调用 adb，Windows PnP 即读取到 USB\\VID_18D1&PID_D002\\MOSQUITO-T113-DEV、Status=OK、Service=WINUSB、DriverInfPath=winusb.inf、ProblemCode=0。USB 产品名仍为 Mosquito T113 v5.6.2 adb-on validated，是描述符文字，不作为当前实板镜像版本证据。这是在已配置开发环境的本机上的结果，不能替代全新电脑免驱验收。

依据：[Microsoft USB 标识](https://learn.microsoft.com/en-us/windows-hardware/drivers/install/standard-usb-identifiers)、[WinUSB 自动安装条件](https://learn.microsoft.com/en-us/windows-hardware/drivers/usbcon/automatic-installation-of-winusb)、[ADB 组成](https://developer.android.com/tools/adb)。

## 历史记录

- 直连查询本机 SQLite 已保存记录；远程查询云端。
- 首次默认全部设备、今天、全部状态；快捷时间有今天、昨天、近 7 天、自定义。
- 设备编号、日期、状态修改后显式点击查询。日期含结束日，固定按北京时间，不跟随电脑时区。
- 每页 50 条，按采集时间从新到旧。单击行自动加载详情；切换时立即清空旧图，取消旧请求，防止响应乱序串图。
- 详情含照片、设备编号、采集编号、采集/接收时间、时间来源、触发来源、温湿度、电源、照片关联 GNSS 位置及采样时间。缺失信息明确说明。
- “完整 / 部分完成 / 失败”为记录状态；上传失败另标阶段，不能把有照片的上传失败说成拍照失败。
- 支持原图缩放、100% 大小和保存照片；不批量打包照片。
- 导出当前查询的整个完整快照，跨全部分页；CSV 包含北京时间及时间来源。旧 API 达到 500 条时提示不完整并禁用完整导出，用户可缩小范围。
- 两种模式分别保存当前筛选、页码、选择；在工作区与记录页之间导航不会丢失。这里指本次运行内，不承诺重启恢复。
- 自动检查发现变化仅提示“有新记录，刷新查看”，不会替换正在查看的历史照片或自动重排记录。

## 上报检查与提示

- 独立于采集记录；只在远程模式显示，默认近 24 小时。可以按设备、日期和上报状态查询。
- 计划由服务端确认已经生效，周期 60 分钟，以设备计划锚点为准，不假设整点。生效前不追溯漏报。
- 宽限固定 15 分钟。时段 T 之前为未到时间；T 到 T+15 分钟前等待上报；截止后没有收到可用记录为超时未上报。
- 服务端确认完整或部分完成的记录都算收到；仅开始上传不算收到。T+15 分钟前收到为按时，恰好截止或之后收到为延迟补报。
- 迟到记录更新原时段，去重；不能把较晚时段的照片拿来填补旧缺口。计划、时间或关联不可靠时显示待确认。
- 表格展示计划时间、编号、状态、实际接收时间和说明。收到记录的时段可进入关联详情；漏报无照片入口。
- 工作区以黄色行内提示显示选中设备的超时时段。总览按近 24 小时统计有超时的不同设备数量，可进入检查。
- 每 60 秒检查一次；已查询的上报检查页更新原查询范围内的状态，保留可保留的选择。历史记录只提示更新。
- 断网保留已显示数据和检查时间，明确提示暂停；无声音、强制弹窗或推送，不把休眠、漏报等同硬件故障。

## 接入边界

客户端已经提供以上界面、查询与兼容逻辑。服务端设备目录、分页快照、稳定编号、计划和检查数据尚需按 [接口约定](HISTORY_REPORT_API.md) 实现并联调；404/501 时显示待确认，不从历史当前页推算漏报。

2026-09-09 当前直连基线已经更新：开发板运行 `dev-v5.6.4-client-direct`、板级包 `2.18-1` 并声明 metadata v3；`mosquito-capture`、`mosquito-environment`、`mosquito-power` 均从 `/usr/bin` 运行，生产客户端的 `BoardCommandPath` 为 `/usr/bin:/bin`，不依赖已删除的 `client-demo`。真实 WPF 已通过登录、检测、读取、手动焦距 500 采集、两次 ADB pull、SQLite PendingUpload、历史及预览回归。自动对焦光学效果仍因相机未固定而延期。

以下是 2026-09-08 v5.6.3 联调前的历史快照，保留用于说明当时为何不能验收，不能再视为当前状态：当时板端仍为 v5.6.3，metadata v3 和传感器命令只在临时路径验证，客户端也尚未用真实开发板完成拍照。板端镜像制作与烧录一直由 WSL 板端流程负责，不由本 Windows 客户端仓库执行。

## 验证与入口

- `dev.ps1 Build`：整个解决方案 Release 编译。
- `dev.ps1 Test`：元数据、SQLite、分页 553 条、CSV、时区、15 分钟边界、部分收到、迟到去重、计划待确认等测试。
- `dev.ps1 UiTest`：WPF 模拟交互、照片请求乱序、模式与导航状态、断网保留、关联记录跳转、离线直连采集落盘；截图在 `artifacts/ui-check/`，数据明确为模拟。
- `dev.ps1 Publish`：可执行文件在 `artifacts/publish/win-x64/MosquitoCapture.exe`；配置文件与其同目录。
- 2026-09-09 成品实板：v5.6.4 真实 WPF 四个生产按钮、手动焦距 500、metadata v3、环境/电源 WARN 展示、两次 pull、SQLite 和预览均通过；自动对焦光学未测。

原第二版 HTML 草图仍在上一级 `design-previews/`，本轮实现位于当前 Windows 客户端代码中。
